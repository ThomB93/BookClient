<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="BookClient.EditBookPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Title" />
            <Entry Text="{Binding Title}" Placeholder="Title" />
            <Label Text="Title" />
            <Entry Text="{Binding ISBN}" Placeholder="ISBN" />
            <Label Text="Title" />
            <Entry Text="{Binding Description}" Placeholder="Description" />
            <Button Text="Save" Clicked="Update_Book"></Button>
            <Button Text="Delete" TextColor="Red" Clicked="Delete_Book"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>